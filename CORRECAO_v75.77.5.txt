Ninou v76.0.0 — correção real da inicialização

Causa do erro:
- A lógica de agrupamento cronológico usava `45 * minute`, mas `minute` não existia no núcleo.
- O ReferenceError acontecia no primeiro render da órbita e interrompia toda a inicialização do app.

Correções:
- Substituído por 45 * 60 * 1000 milissegundos.
- Mantida a órbita cronológica de 24 horas da v75.77.4.
- Cache do Service Worker recebeu nome exclusivo da v76.0.0.
- Nenhum dado local, familiar ou do Firebase é apagado.
