Revisão v75.75.44 — convites por família

Escopo validado:
1. Convite criado a partir da família atual.
2. Campo de e-mail do convidado no Perfil.
3. Seleção de permissão: responsável, cuidador ou visualização.
4. Código único com validade de 7 dias e uso único.
5. Salvamento em invites/{code} para descoberta por código.
6. Salvamento em families/{familyId}/invites/{code} para painel familiar.
7. Salvamento em families/{familyId}/invitations/{code} para compatibilidade com versões anteriores.
8. Aceite de convite vinculando users/{uid}/families/{familyId}, users/{uid}/access/ninou e families/{familyId}/members/{uid}.
9. Cache local de convite ativo separado por familyId.
10. CSS único preservado.

Fora do escopo desta versão:
- Não altera regras Firestore.
- Não cria cobrança/assinatura.
- Não implementa painel financeiro/comercial.
